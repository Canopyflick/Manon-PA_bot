---
description: Obsidian vault management for PA_bot (Diary daily notes, Windows git mirror)
globs: ops/raspberry-pi/obsidian-vault/**
alwaysApply: true
---

# Obsidian Vault (ops/raspberry-pi/obsidian-vault)

Vault lives on OneDrive, **outside** this repo. Daily notes: `Diary/`. Skill: `.cursor/skills/obsidian-vault/SKILL.md`.

## OneDrive first (mandatory)

**OneDrive is the source of truth** for vault content. Git (`obsidian-vault-backup`) is version history only.

Before **any** vault read that informs deletes/edits, or **any** file mutation (create, edit, delete, rename):

1. **Windows:** `wait-onedrive-vault.ps1` (or rely on `vault-git-snapshot.ps1`, which calls it automatically).
2. **Pi:** `ssh ben@raspberrypi ~/obsidian/scripts/obsidian-sync-onedrive.sh`
3. **Git:** `vault-git-snapshot.ps1` runs `git fetch` + `git rebase` onto `origin/main` before commit/push. Do not use raw `git` for vault backups unless you mirror that flow.

Never delete, bulk-edit, or snapshot-push vault files on a stale local tree.

## Paths

| Environment | Vault | Git metadata |
| --- | --- | --- |
| Windows | `C:\Users\Ben\OneDrive\Obsidian\hej` | `%USERPROFILE%\.obsidian-vault-backup\vault.git` |
| Raspberry Pi | `/home/ben/obsidian/vault` | `/home/ben/obsidian/backup-git/vault.git` |

Local config (gitignored): `ops/raspberry-pi/obsidian-vault/config.env` — copy from `config.example.env`.

Pi sync/backup runbook: `ops/raspberry-pi/docs/obsidian-vault-backup.md`.

## Scripts

| Script | Use for |
| --- | --- |
| `wait-onedrive-vault.ps1` | **Run first** — OneDrive preflight before any vault edit (also called by snapshot) |
| `vault-git-bootstrap.ps1` | One-time Windows git mirror setup |
| `vault-git-snapshot.ps1` | Commit before/after vault edits (`-Push`); auto OneDrive wait + `git pull --rebase` |
| `analyze-safebackup-duplicates.py` | Compare `safeBackup` copies vs canonical notes |
| `analyze-diary-headers.py` | Read-only Diary header/scaffold scan |
| `fix-diary-nav.py` | Fix wrong dates on existing nav lines only (never adds lines) |

## Git safety

Always run `vault-git-snapshot.ps1` **before and after** any vault file mutation from Cursor. Never add `.git` inside the OneDrive vault folder.

Before **push**: `git --git-dir=… --work-tree=… pull --rebase origin main` (or use snapshot only after pull). Pi nightly backup also pushes to the same repo — rebasing first avoids modify/delete conflicts on files Pi still had (e.g. `safeBackup` copies).

## Agent conventions

Read `obsidian-vault/docs/working-in-the-diary.md` before editing Diary notes.

## Analysis output

Reports go to `obsidian-vault/reports/` (gitignored). Committed taxonomy summary: `obsidian-vault/docs/analysis-taxonomy.md`.
